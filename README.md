# Authentication and Authorization Microservices
- A backend system that manages who users are (AuthN) and what they can do (AuthZ), typically using logins,
  tokens, roles, and protected API endpoints.
